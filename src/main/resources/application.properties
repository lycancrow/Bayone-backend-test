server.port=${PORT:8080}

# Datasource para SQL Server (corrige host/usuario/clave según corresponda)
spring.datasource.url=jdbc:sqlserver://bayone-server-database.database.windows.net:1433;databaseName=BayOneDatabase;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=bayoneadmin@bayone-server-database
spring.datasource.password=BayOne2025*
# opcional, normalmente se autodetecta si el driver está en el classpath:
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
